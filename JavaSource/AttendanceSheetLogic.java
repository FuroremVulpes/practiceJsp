package model;

public class AttendanceSheetLogic {

/*
	public List<Attendance> getListAttendance(String userId, int year, int month){

		AttendanceDAO dao = new AttendanceDAO();
		List<Attendance> listAttendance = new ArrayList<>();
		listAttendance = dao.getAttendance(userId, year, month);

		return listAttendance;
	}


	public boolean excuteAdd(Attendance attendance){
		AttendanceDAO dao = new AttendanceDAO();
		boolean isResult = dao.addAttendance(attendance);
		return isResult;
	}

	public boolean excuteUpdate(Attendance attendance){
		AttendanceDAO dao = new AttendanceDAO();
		boolean isResult = dao.updateAttendance(attendance);
		return isResult;
	}

	public boolean excuteDelete(Attendance attendance){
		AttendanceDAO dao = new AttendanceDAO();
		boolean isResult = dao.deleteAttendance(attendance);
		return isResult;
	}
*/
}
